[defaults]
forks = 20
# Copy/symlink the private key for the ubuntu user in the below location
# or edit these as appropriate
private_key_file=~/.ssh/cdh_cluster_id_rsa

[ssh_connection]
control_path = %(directory)s/%%h-%%r
